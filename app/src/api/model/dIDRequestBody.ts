/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * Economicon App API
 * OpenAPI spec version: 0.3.0
 */
import type { MissingValueHandlingType } from './missingValueHandlingType';
import type { StandardErrorSettings } from './standardErrorSettings';

/**
 * 差の差（DID）分析リクエスト

Two-Way Fixed Effects（TWFE）による差の差推定と、
オプションで Event Study を実行します。
交差項（treated × post）はサービス層で自動生成します。
 */
export type DIDRequestBody = {
  /**
     * 分析対象のテーブル名。ワークスペース内に存在するテーブル名を指定してください。
     * @minLength 1
     */
  tableName: string;
  /**
     * 分析結果の名前（省略時は自動生成）
     * @maxLength 128
     */
  resultName?: string;
  /**
     * 分析結果の説明メモ
     * @maxLength 512
     */
  description?: string;
  /**
     * 被説明変数（目的変数）の列名。テーブル内に存在する数値型カラムを指定してください。
     * @minLength 1
     */
  dependentVariable: string;
  /** 追加の共変量（コントロール変数）の列名リスト。treatment_column / post_column との重複は不可。 */
  explanatoryVariables?: string[];
  /**
     * 処置群ダミー列名（treated_i）。個体レベルの 0/1 変数。処置群=1、対照群=0。
     * @minLength 1
     */
  treatmentColumn: string;
  /**
     * 処置後ダミー列名（post_t）。時点レベルの 0/1 変数。処置後=1、処置前=0。
     * @minLength 1
     */
  postColumn: string;
  /**
     * 時点列名。数値型または Date 型。Event Study 実行時は整数型を推奨します。
     * @minLength 1
     */
  timeColumn: string;
  /**
     * 個体 ID 列名。TWFE の個体固定効果に使用します。
     * @minLength 1
     */
  entityIdColumn: string;
  /** Event Study を実行するかどうか。True の場合、各時点の処置効果係数（δ_k）を推定します。 */
  includeEventStudy?: boolean;
  /** Event Study の基準期（処置効果をゼロに固定する時点）。null の場合は処置直前期（通常 -1）を自動選択します。include_event_study=true のときのみ有効です。 */
  basePeriod?: number | null;
  /** Event Study で表示する処置前の最大期間数。null の場合はデータ内の全処置前期間を使用します。 */
  maxPrePeriods?: number | null;
  /** Event Study で表示する処置後の最大期間数。null の場合はデータ内の全処置後期間を使用します。 */
  maxPostPeriods?: number | null;
  /** 欠損値の処理方法。remove: 該当行を削除、ignore: そのまま使用、error: エラーとして扱う。 */
  missingValueHandling?: MissingValueHandlingType;
  /** 標準誤差の計算方法。DID では個体レベルのクラスタ標準誤差（cluster）を推奨します。 */
  standardError: StandardErrorSettings;
  /**
     * 信頼区間の水準（デフォルト: 0.95）
     * @minimum 0.5
     * @maximum 0.999
     */
  confidenceLevel?: number;
};
