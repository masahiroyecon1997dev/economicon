/**
 * Generated by orval v8.5.1 🍺
 * Do not edit manually.
 * Economicon App API
 * OpenAPI spec version: 0.1.0
 */
import type { ExportFileRequestBodyEncoding } from './exportFileRequestBodyEncoding';
import type { ExportFileRequestBodyFormat } from './exportFileRequestBodyFormat';

/**
 * ファイルパス指定エクスポートリクエスト（CSV / Excel / Parquet 共通）

``format`` に応じて適切なエクスポーターが自動選択され、
ファイル拡張子も自動で付与されます。

- ``csv``     →
    CSV エクスポーター（separator / encoding / include_header が有効）
- ``excel``   → Excel エクスポーター（sheet_name が有効）
- ``parquet`` → Parquet エクスポーター
 */
export type ExportFileRequestBody = {
  /**
   * エクスポートするテーブル名。ワークスペース内に存在するテーブル名を指定してください。
   * @minLength 1
   */
  tableName: string;
  /**
   * 出力ファイルのディレクトリの絶対パス（相対パスはサポートされていません）。
   * @minLength 1
   * @maxLength 1024
   */
  directoryPath: string;
  /**
   * 出力ファイルのベース名（拡張子なし）。同名ファイルが存在する場合は上書きされます。
   * @minLength 1
   * @maxLength 255
   * @pattern ^[^\\/:*?"<>|]+$
   */
  fileName: string;
  /** 出力するファイル形式。csv / excel / parquet から選択してください。選択した形式に応じた拡張子が自動付与されます。 */
  format: ExportFileRequestBodyFormat;
  /**
   * CSV 区切り文字（CSV のみ有効）。カンマかタブ、もしくは 1〜10 文字の任意の文字列から選択してください。
   * @minLength 1
   * @maxLength 10
   */
  separator?: string;
  /** CSV ファイルの出力エンコーディング（CSV のみ有効）。utf8 / latin1 / ascii / gbk / windows-1252 / shift_jis から選択してください。 */
  encoding?: ExportFileRequestBodyEncoding;
  /** ヘッダ行を含めるか否か（CSV のみ有効）。デフォルトは True（ヘッダあり）。 */
  includeHeader?: boolean;
  /** 出力する Excel シート名（Excel のみ有効）。省略または null の場合は 'Sheet1' を使用します。 */
  sheetName?: string | null;
};
