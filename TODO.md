# TODO

## アプリ名

Economicon

## 画面

**ファイルインポート画面**

- ドラッグ&ドロップでファイルをインポートする画面を作成（Rust側ではファイルパスのみでAPIに渡す）
- 現在のファイル選択画面も存続。ファイルインポート画面はタブでドラッグ&ドロップ画面とファイル選択画面を切り替えられるようにする
- ファイル選択画面は詳細を設定してインポートする画面をモーダルで作成

**シミュレーションデータ**

- 各ボタンのカーソルをポインターにしたい。
- 設定値があまりにシンプルすぎる。せめて列名、データ形式、分布、パラメータがわかるようにしたい。
- 4列が見えない
- シミュレーションデータにIDを割り振れる値を追加（APIの変更も必要）
- 固定値は数値のみにする。現状は文字列として列に追加されてしまう。これは修正。

**テーブル**

- 仮想テーブルを実装したが、初期表示時にデータが表示されないバグが発生。理由は不明。コードを見てバグを直す。
- カラムの名前の部分に関して、数値型や文字列かどうかがわかるようにするとよい。

**回帰分析入力フォーム**

- 選択済み説明変数の視認性: 下の「未選択」エリアに、選択した変数名を 「タグ（チップ）」 形式で表示し、×ボタンで削除できるようにすると、長いリストをスクロールしてチェックを外す手間が省けます。
- 検索/フィルタ: 列数が多いデータ（100列など）を扱う場合、リストの上に小さな検索窓（Filter columns...）があると親切
- constの扱い: リストの中に const が見えますが、これはデータに含まれる列ですか？もし自動で追加する定数項なら、変数リストからは除外して「詳細設定」のチェックボックスに追い出したほうが、ユーザーの混乱を防げます。
- 詳細設定オプションの追加
  - アコーディオンメニューを説明変数・被説明変数選択の下に配置
  - 標準誤差の計算方法: ドロップダウン（通常、頑健、クラスター頑健）
  - 定数項の有無: チェックボックス（デフォルトON）
  - 欠損値処理
  - 手法特有のものは表示を切り替え
    - Tobit: 左側打ち切り値、右側打ち切り値
    - パネルデータ: 個体ID列の選択、時間列の選択

**ジョイン・ユニオン**

- 画面作成

**モード選択**

- 設定画面にてモード選択ができると良い（ライトモード、ダークモード、独自モード（ちょっと面白く））

**メニュー**

- 縦の三点リーダーとコンテキストメニュー両方実装するのがよい。
- テーブルの名前とカラムの名前の変更をダイアログではなく、Input要素に変更して変更できるようにするかどうか

**グラフ・図**

- Recharts plotly.jsなどを使用して実装。
- どのように実装するかは未確定

**フォーム**

- zodを有効活用してできる限りこれでバリデーションを行いたい。独自実装のバリデーションが可能かどうかも調べる。
- react hook formからTanstack formに乗り換えてみては？

## REST API, Python API

**リファクタリング**

- シングルトンから依存性注入に変更可能かどうか検討　優先度：低
- pydanticをPython APIにも適用　優先度：低
- ファイルリストを取得するAPIをRust側に移行　実施済み　要動作確認
- 操作変数法にGLSやGMMは必要か、不要なら2SLSのみでOK
- Any をなくす

**新機能・新API**

- ダミー変数を一気に作成するAPI（列の値をuniqueにして, 複数値対応、数値範囲対応）
- 予測値・残差を計算するAPI
- 予測値・残差はシリアライズ化したモデル（statsmodels, linearmodels）をデータとして保存。計算にはそれを取り出して実施。ファイルの場所はanalysis_data_storeにuuid.pklのような感じで保持
- パスによるインポート
- 回帰分析等の結果の返し方だが、uuidによる入れ方ではなく、名前で識別する形にしたい。（いずれスクリプト化するときのため）
- polarsの列の型変換
- 列追加時にデータを追加
- 1行目をヘッダに昇格するapi
- 因果推論系（DID, RDD, RCTなど）
- Ridge, Lassoの標準偏差計算方法を修正。（ブートストラップによる係数の標準偏差はいいものの、ここからp値）
- 統計検定機能（Z 検定、t 検定、F 検定）
- 列追加時にデータ挿入ができるように
- ファイルインポート・エクスポートのパスのアクセス権・書き込み権限・存在するかどうかについても検証するコードをつける
- 列追加系のAPIにどの位置に追加するかのキー列指定がない
- シミュレーションデータの追加系のAPIのカラム名指定をどのように行うか
- loggerの変更（出力内容と使用しているパッケージの変更）
- 回帰分析APIにてリクエストボディの階層構造化を行う。あとHACやclusteredに追加で必要なパラメータ検証も含めること
- LazyFrameへの変更

**テスト**

- ## エラーメッセージ
  - test_sort_empty_columns（空のソート列指定）のエラーメッセージのテストを行う
  - エラーメッセージの完全一致を検証
- 分析結果の妥当性検証を行う（回帰分析、FE、IV等に対して）。
  1. 同じ分析方法（statsmodels, linearmodels）で10回シミュレーションデータを生成して値がAPIから来たものと変わらないことを確認
  2. Rとのパラメータ比較（Rでの分析結果との突合せ、wooldridge等のデータを使うもの3つとシミュレーションデータによるもの2つ）
- テーブル名重複（今は別名を付ける形にしているが、これだと何かの拍子に被る or 繰り返しが多くなるのでエラーにする。そのテスト）
- カラム名重複（これもエラーにする。理由はテーブル名と同じ。ファイルからの取り込み時は確認はpolarsに任せる。）
- ## テストカバレッジの向上
  - 制御文字でのエラー、境界値検証、特殊文字、空文字、エッジケースの検証
- pydanticの型のテスト
  - パターン1：文字列の "true" を送ってみる（バリデーションの確認）
  - パターン2：数値の 1 を送ってみる
  - パターン3：生（Raw）の JSON 文字列で送ってみる

## インストーラ作成

**Inno Setupの使用**

- Inno Setupを使用してインストーラを作成する方法を検討。tauriでも可能なようだが、Embedded Pythonの方法で可能かどうかが不明。

## その他

**CI/CD**

- 脆弱性に関してgithubのCI/CDを利用する。
- 依存性注入による変更
- Rustでトークンを作成してapi側に渡す（ここは相談して実装案を検討）
- ポート番号の割り振りについてもRust側で行った方がいいかも、でプロンプト上でAPI側に渡すことにしてもよい（ここは相談）
- tauriからの外部通信を不可にするように
- **README.md**

  **Docs**

- Swagger/OpenAPI ドキュメント拡充 (FastAPI の自動生成機能活用)

**CI/CD**

- CI/CD パイプラインの構築 (GitHub Actions 導入済み)
- workflowsでテストからリリース（配布用exe作成）までを行ってくれるものを作成
- issueから情報を抜いて実装計画を行い、プロンプトを書きだすスクリプトの作成　Github CLIを使用
